形態素解析器の初期化の時間を省いたり、
毎度毎度プログラムを動かすのが面倒な場合に使う、フォルダ常駐型の形態素解析プログラムです。

/
 -janome_watchdog.py(メインプログラム)
 -text(入力テキストを渡す用のフォルダ)
 -out(形態素解析の結果出力)

使い方は単純に、
①プログラムを実行
②形態素解析したい文章をUTF-8でテキストファイル化し、textフォルダへ移動
③自動で解析が走り、標準出力やoutに結果が出力される
といった感じです。

ただし、入力テキストファイル名は「str_」で始まる必要があります。